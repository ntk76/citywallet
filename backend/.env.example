PORT=8787
# Placeholder only — never commit a real key. Create .env locally and paste your key from https://tavily.com/
TAVILY_API_KEY=your-tavily-api-key
CACHE_TTL_MINUTES=20
TAVILY_TIMEOUT_MS=6000

# Optional: override city/region/timezone for Tavily “events today” query (see application.properties)
# EVENTS_SEARCH_CITY=Stuttgart
# EVENTS_SEARCH_REGION=Mitte
# EVENTS_TIMEZONE=Europe/Berlin
